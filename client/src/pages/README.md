# Pages Directory

This directory can be used for:
- Additional utility pages
- Documentation pages
- Static pages that don't need routing

Note: Since we're using Next.js App Router, main routes are in `src/app/`.

