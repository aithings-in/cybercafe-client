# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/cybercafe
# Or use MongoDB Atlas connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cybercafe?retryWrites=true&w=majority

# CORS Configuration
CLIENT_URL=http://localhost:3000

# Google Drive Configuration
# Option 1: Service Account Key File (Recommended)
# Download service account JSON from Google Cloud Console and provide path
GOOGLE_DRIVE_KEY_FILE=./path/to/service-account-key.json

# Option 2: Service Account JSON as Environment Variable (Alternative)
# GOOGLE_DRIVE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Option 3: OAuth2 Client (For user-based authentication)
# GOOGLE_DRIVE_CLIENT_ID=your-client-id
# GOOGLE_DRIVE_CLIENT_SECRET=your-client-secret
# GOOGLE_DRIVE_REDIRECT_URI=http://localhost:5000/auth/google/callback
# GOOGLE_DRIVE_REFRESH_TOKEN=your-refresh-token

# Optional: Specific folder ID where files should be uploaded
# If not provided, files will be uploaded to root or user-specific folders
GOOGLE_DRIVE_FOLDER_ID=1EwzKQNgXgwuCbEvAEI0Rnx4K4aB8TW2F

